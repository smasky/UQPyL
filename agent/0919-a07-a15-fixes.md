# A07—A15 修复记录 · 2026-09-19

用户授权直接完成剩余代码审查问题。本次沿用 conda py312，保留工作区之前的 LBFGSB、A01—A06 修复，没有新增依赖。

## 修复内容

| 项目 | 实施结果 |
|---|---|
| A07 | DeltaTest 构造验证 nNeighbors 为非布尔正整数；实际邻居搜索验证 nNeighbors<nSamples。删变量敏感性分析明确要求至少两个输入；一维输入不再进入零列 KDTree。内部组合搜索仍允许单列邻居搜索，不为一维敏感性指标引入未经确认的数学定义。 |
| A08 | rank_score 使用 SciPy Kendall tau-b，逐输出计算后等权平均；支持一维单输出，检查行列匹配和有限值。常数列贡献 0；不足两样本明确报错。此举同时明确 ties 规则，不再把旧自定义成对比较称作标准 Kendall 指标。 |
| A09 | smooth_curve 校验一维输入和正整数窗口；空/单点/短于窗口的序列返回副本，长度不变。一般序列保留既有卷积及边界处理。 |
| A10 | DEMC 默认 nChains=3，构造时拒绝小于 3、布尔和非整数值，原运行时下限检查保留。 |
| A11 | 代理辅助优化每次拟合从外层 RNG 派生子 seed；MultiSurrogate 再为每个子模型派生独立 RNG。相同初始模型配置、数据和外层 seed 可复现；同一已拟合模型重复运行仍需考虑参数已改变，不承诺仅重设 seed 即回到初始状态。自定义代理需使用其 rng 才能继承此规则。 |
| A12 | analysis 顶层复用 methods 的可选 MARS 导出，去掉重复广义异常捕获；可选组件只容忍已知编译扩展命名空间的 ModuleNotFoundError，其他导入错误直接抛出，surrogate 层同步收窄。AutoTuner 共用候选评分路径，仅捕获 LinAlgError/ArithmeticError；非有限预测或评分作为数值失败，记录 candidateFailures 而非 print。TypeError/AttributeError/普通 ValueError 直接传播。 |
| A13 | 删除 AMH/DEMC 未用于算法的 _check_alpha，以及仅转发的 check_bound；调用现有基类边界检查。删除 KRG 无用 GA/r_square/RandSelect 导入。原测试中仅为旧方法提供覆盖的部分改为测试真实 _check_gamma_ 路径。 |
| A14 | BaseReader.list_runs 返回 run_id、created_at、finished_at、final_fes、final_iters、db_path、file_name（按领域适用）。保留数据库列名、get_run 原始行协议和内部字段；更新真实消费者测试，不保留旧导出别名。内部风格按实际触达整理，不全仓机械重命名。 |
| A15 | 7 个 DOE 示例的旧 UQPyL.problems 导入改为 UQPyL.problem，并实际执行 doctest。FAST 示例增加采样数以避免示例本身产生辅助频率重用警告。rank_score/sort_score 返回注解与实现同步；DEMC 文档默认值同步。 |

中英文 analysis、surrogate、optimization、inference、calibration API 已同步上述行为。A08 的常数列规则、A11 外层随机流覆盖内置代理的 RNG，以及 A14 的导出字段变化均已写明。

## 验证

- [新增 39 项回归](../tests/test_review_a07_a15.py)：DeltaTest 边界、Kendall ties/多输出、7 种平滑长度、DEMC 默认实际运行与非法参数、多模型随机流、两套全新 ASMO/KRG 实例的真实评价及预测复现、两种调参入口的编程错误传播、数值失败诊断、可选导入故障注入、7 组 DOE doctest、真实 SQLite 导出字段。
- 第一轮相关测试 110 项通过；新增文档与字段完整覆盖后的专项 39 项通过。首轮全量发现旧测试仍调用已删除 _check_alpha，已改为基类实际使用的 _check_gamma_，并通过相关测试。
- 最终全量 **1498 passed，4 条原有 FAST warnings，22.66 秒**；日志：[0919-a07-a15-pytest.txt](verification/0919-a07-a15-pytest.txt)。

本轮未提交推送，未将本地测试当作远程 CI 结果。
