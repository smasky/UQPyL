# 2026-09-18 开发交接与复盘

本文件用于换电脑后继续 UQPyL 开发，依据本轮对话、仓库代码、审查记录和测试结果整理。它是决策纪要与工作总结，不是完整逐字聊天导出；旧审查报告保留了当时的缺陷描述和阶段状态，不能将其中的历史“未完成”直接当作今天的状态。

## 1. 当前目标与工作基线

- 用户明确以本地开发实现为准，本次授权将源码、测试、文档和交接记录保存到 GitHub，便于换电脑接续工作。
- 仓库：<https://github.com/smasky/UQPyL>；目标分支：`dev`。只做开发提交，不创建 `v*` 标签、不创建正式 Release、不触发 PyPI 发布。
- 当前包元数据仍为 `2.1.6`；这次不调整版本号。不能仅凭版本字符串判断代码新旧，应看分支和提交记录。
- 最低 Python 版本已调整为 3.10；本机使用 conda `py312`。本地测试通过不代表全部 Python/操作系统组合均已通过。
- 第二轮审查 N01—N20 已完成，S01—S06 精简建议已落实。D01 已确认保留 Boxmin，MP 默认 4 次重启已落实，见 [补充实验](0918-d01-restarts.md)；D02 已确认按迭代边界检查，D03 已采用入口报错策略。
- 不增加新的运行时第三方依赖。代码改造优先处理数值正确性、输入输出边界、真实模型调用次数、结果隔离和重复实现。

## 2. 已确认的设计决定

| 主题 | 当前约定 | 接续开发注意事项 |
|---|---|---|
| 两类问题 | `Problem` 用于普通函数评价；`ModelProblem` 连接模型仿真与观测/指标评价 | 两者正式返回协议都是 `Eval`，用 `.objs`、`.cons`、`.sims` 访问 |
| `sims` 必要性 | `ModelProblem` 的 `target=None/sims` 按模拟契约返回；`target=objs/cons` 不强制在返回对象中带 `sims` | 内部仍可能需要仿真来计算目标；不要把“不返回 sims”误解为“不运行模型” |
| 按需评价 | 独立目标/约束回调按 `target` 执行 | 请求目标时不额外调用约束回调，反之亦然 |
| 核函数归属 | 类外核实例是构建模板，模型安装时复制 | 多模型不会通过同一核的可变参数相互污染；自定义核的外部资源可通过 `clone` 定义复制策略 |
| Matérn | 固定 `nu` 支持一般正值，并保留常用值/无穷极限专门路径；可调 `nu` 仍是规定候选集 | 一般正值的数值稳定性已修正，大 `nu` 的稳定积分路径较慢；不要宣传为任意规模下零成本 |
| 优化坐标 | 普通优化和代理辅助优化内部统一 `[0,1]`；真实模型评价经 Problem/Space 解码 | 代理辅助算法在单位空间训练/预测；独立使用代理模型仍可直接传自己的数据和 Scaler |
| 采样坐标 | DOE 默认输出真实值，算法可指定 `output='unit'` | 不应把真实采样结果再次当成编码值解码 |
| 推断坐标 | 推断保留连续潜在状态，评价与结果出口解码真实参数 | 推断所有连续轴没有统一改为 `[0,1]`；不能把优化约定机械套到贝叶斯算法 |
| 输出与持久化 | 优化结果出口恢复真实参数与原始目标方向；过程统计、种群快照、数据库保存有各自语义 | Reader 只能恢复实际保存过的快照，不能补出未保存的迭代 |
| 约束 | 可行解优先；不可行解按加权违反量比较；权重由 Problem 贯通种群、排序、结果与存储 | 多目标可行前沿和不可行候选分开；等违反量排名与历史最优比较使用一致规则 |
| MOASMO | 当前按无约束问题定位 | 用户明确暂不讨论约束代理/约束候选引导，不能把 M08 列为本轮遗漏的必修缺陷 |
| Scaler | 保留可选 Scaler，修正方差逆缩放、常数列等边界 | AutoTuner 先划分训练/验证，再拟合预处理，防止数据泄漏 |
| 校准指标 | 继续依据 metric label 决定方向 | `normalizedScore` 用于统一选优方向，不是把分数数值缩放到 `[0,1]` |
| PBIAS | 展示保留原始带符号值，选优按距零比较 | PBIAS 阈值为双侧范围；不擅自把原始指标公式变成绝对值 |
| 兼容策略 | 开发阶段暂不要求向后兼容 | 已移除旧 transform 包装和部分转发/别名，旧脚本需要同步正式接口 |

## 3. 已完成的工作

### 前一轮整体审查及专项修复

- R01—R18 的代码修正已落实；R14 仍需远程 CI 实跑，R15/R16 的线上文档部署与正式发布没有执行。
- 修正默认组件共享、显式核模板复制、MultiSurrogate 列表容器隔离。
- 检查并修正 GP/Kriging/RBF 核的符号、形状、固定参数、参数域、零距离及一般 Matérn 数值稳定性。
- 优化统一单位坐标，推断修正混合变量重复解码及评价/记录不一致；EGO 对接真实 KRG 预测接口。
- 贯通约束权重，修正不可行最优比较、多目标档案、HV 参考点/尺度和若干退化种群边界。
- 修正 Scaler 方差量纲、AutoTuner 预处理泄漏及数据划分随机流；修复校准指标方向、PBIAS 语义与 ES/IES 秩不足协方差求解。
- 四领域增加异常收尾、失败状态与连接关闭；分离优化轻量统计、内存种群快照和 SQLite 保存频率；高维 HV 改为分块计算。
- Python 支持声明、干净 wheel 测试 CI、文档入口与 sdist 发布衔接已调整。

### 第二轮逐项修复 N01—N20

| 编号 | 已完成内容 |
|---|---|
| N01 | GPR 统一最小化负对数边际似然，记录与选优方向一致 |
| N02 | Boxmin 边界修正；Boxmin/LBFGSB 使用局部随机流 |
| N03 | RBF 平滑项只作用于正确核块，保持多项式趋势约束 |
| N04 | AutoTuner 按维度切片应用向量超参数 |
| N05 | Lasso 使用独立工作数组，避免修改调用方训练数据 |
| N06 | DREAM_ZS archive 复制历史点，避免共享当前链视图 |
| N07 | 修正 14 个优化器的停滞判定时点、迭代计数与上限边界 |
| N08 | 校准结果深复制 history/diagnostics/extra/settings，避免复用污染 |
| N09 | 敏感性分析不再把小量纲非恒定输出误判为常数，保护极端尺度 |
| N10 | LHS correlation 按变量列计算，一维/单样本回退普通 LHS |
| N11 | Space 在整数转换前验证 varType，拒绝静默截断与非法类型 |
| N12 | 分析入口统一 X/Y 规整、样本对应关系、输出列选择和标签 |
| N13 | 校验预评估 initialPop；必要约束缺失立即报错，完整数据不重评 |
| N14 | ES/IES 复用完整模拟，SUFI2 从已算模拟中提取 elite，减少真实调用 |
| N15 | Reader 恢复真实 OptResult/OptHistory，修正缺失 HV 后横坐标错位 |
| N16 | 每次运行使用统一 UUID runId；日志不覆盖；失败清理不污染旧记录 |
| N17 | 保存/恢复简单算法配置，用 literal_eval 替代 eval，组件恢复限制明确警告 |
| N18 | 数据库记录领域标识；Reader 校验并筛选所属模块 |
| N19 | 优化全过程与最终出口刷新耗时，内存/SQLite/显示一致 |
| N20 | problem.__all__ 使用扁平字符串列表，公开导出验证通过 |

### 精简 S01—S06

1. GPR/KRG 只预测均值时跳过方差计算；GP 自核只取必要对角，自定义核有界分块。
2. 校准保存一份结果加小摘要；优化数组以成员表为主，避免重复存入 JSON。
3. Reader 共用连接、关闭、上下文与基础查询；摘要查询不加载完整大对象。
4. 删除旧 analysis 转发、旧 transform 包装、无用 ensemble 占位和死代码；运行对象统一 `state/params`，返回结果的 `settings` 保留。
5. GPR/KRG/RBF 共用核安装辅助逻辑，同时保持各核数学家族独立。
6. 独立目标/约束回调按请求执行，消除不必要的额外调用。

## 4. 验证证据及边界

- 上一轮最终全量：Python 3.12 下 **1345 passed，4 warnings，17.27 秒**。4 条是既有 FAST 辅助频率复用警告，不是测试失败。
- 本次 GitHub 交接前再次运行全量，结果记录在下方交接验证小节；关键日志另存到仓库，避免只依赖旧电脑的 `/tmp`。
- N11—N20/S01—S06 新增联合回归 61 项；Reader/绘图专项 67 项通过，核/调参/Problem/analysis 联合专项 438 项通过。这些专项与全量有重叠，不应相加为独立测试总数。
- 模型调用计数案例：ES 两批、IES 三轮四批、SUFI2 elite 不重复评价。
- 2000 个预测点的 GP 自核数组：32,000,000 字节降到 16,000 字节。这不是整个预测过程的峰值内存。
- 固定 GLUE 案例序列化 artifact：56,322 字节降到 28,492 字节，约 50.6%。这不是 SQLite 总文件大小比例。
- 尚未完成所有原生扩展的独立内存安全审计、所有 MCMC 提议核的理论/收敛性证明或大规模性能基准；测试通过不能等价为这些证明。

## 5. 尚未完成及需要决定的事项

| 项目 | 当前状态/默认行为 | 下一步 |
|---|---|---|
| D01：默认内部优化器 | 已完成 240 次 GPR/KRG 实测，建议保留 Boxmin；默认值未修改，见 [实验报告](0918-d01-optimizer-comparison.md) | 用户已确认保留 Boxmin；MP 重启方案已落实，见 [补充实验](0918-d01-restarts.md)；全量 1387 项及最终重启专项 32 项通过 |
| D02：maxFEs 预算 | 初始化与已开始轮次完整执行，允许超过 maxFEs；maxIters 独立限制轮次 | 已确认并落实，见 [D02 记录](0918-d02-iteration-budget.md) |
| D03：预评估初始种群缺字段 | 已采用入口报错，不自动追加模型调用 | 无需重新实施；只有用户更改约定时再讨论 |
| 远程 CI | MARS / GCC 15 修复 `c38078e` 已推送，新提交六组 CI 全部通过；本机严格构建及 1357 项 wheel 测试通过，见 [修复记录](0918-mars-gcc15-fix.md) | 3.11/3.13 等声明支持组合仍需发布前验证 |
| 正式版本/发布 | 本次不做，版本保留 2.1.6 | 未来发布时制定版本号、变更说明、迁移说明，并检查发布与测试门禁衔接 |
| 新旧数据库 | 新 Reader 要求 runtimeMeta 领域标识；旧无标识数据库不能直接读取 | 若有必须保留的旧结果，单独设计迁移；本轮没有迁移工具 |
| 算法恢复 | 简单配置可恢复；代理模型、内部优化器等实例需手动重建 | load_algorithm 是配置恢复，不是断点续跑或恢复 RNG/搜索状态 |
| 历史完整度 | historyFreq 与 saveFreq 独立；只恢复实际保存的快照 | 长任务开始前确定记录频率；降频不等于固定内存上限 |
| 历史文档 | 旧报告和 .codex 草稿保留追溯价值 | 以本文件、TODO、最新 API 和测试为准；后续可继续删减冲突旧说明 |
| MOASMO 约束扩展 | 用户明确暂不纳入 | 保留为未来功能选择，不作为当前待修缺陷 |

没有为了本次换电脑而改变上述未定设计。进一步检查建议必须先给证据，再决定是否新增任务，避免将旧报告中的“可能问题”直接算作已确认缺陷。

## 6. 换电脑后如何接续

以下命令在新电脑的新目录执行；已有仓库先确认工作区干净，避免覆盖另一份本地修改。

```bash
git clone --branch dev git@github.com:smasky/UQPyL.git
cd UQPyL
git log -3 --oneline

conda create -n py312 python=3.12 pip
conda activate py312
python -m pip install -e '.[viz]' pytest pytest-cov
python -m pytest -q
```

源码含 Cython/C++ 原生扩展，新电脑需要适合本平台的编译工具链。pip 构建隔离会根据 pyproject.toml 安装构建依赖；不要复制旧电脑的 `.so`、虚拟环境或 Python 缓存。包名/版本仍显示 2.1.6 不代表安装错版本，请同时核对当前提交和 `UQPyL.__file__`。

检查 wheel 可复用 CI 的路径（需要联网安装构建和测试依赖）：

```bash
python -m pip install build
python -m build --wheel --outdir .cache/handoff-wheel
python .github/scripts/test_wheel.py --wheel-dir .cache/handoff-wheel --report-dir .cache/wheel-test
```

wheel 目录应只含当前平台本次构建的一个 wheel，避免混入旧产物。文档源在 `docs_v2/`，配置为 `mkdocs.yml`；可用 `python -m pip install -r docs-requirements.txt` 后执行 `python -m mkdocs build --strict`。

接手顺序：先看本文件和 TODO → 核对 GitHub 当前提交的 CI → 本机重装并完整测试 → 查阅 D01/D02 已确认决定 → 按真实使用案例继续验证。无需从头重复已经关闭的 N 项。

## 7. 对话关键决定摘录

以下为用户原话的选摘，附当前落地含义；省略中间来回，不构造未保存的完整对话。

> “那肯定是以本地端口为主，你先看一遍设计语言，我们再讨论”

以本地实现与确认过的设计作为工作基线，远程版本字符串不自动覆盖本地选择。

> “我同意，我觉得进行复制更好，其实这个类外核函数，没什么用，只是用来创建用”

传入核作为构建模板，安装到模型时复制隔离。

> “我觉得可以，那就按0-1来，那你的方案是什么”

优化内部采用单位坐标；实际参数、独立采样、推断潜在状态仍分别遵循对应边界约定。

> “MOASMO 默认应该是不带约束的问题，这个暂时先不讨论”

MOASMO 约束扩展暂缓。

> “我们目前还是按照metric的label，这里没有问题”

保留指标标签决定方向的现有设计。

> “好的，直接修改为3.10吧”

最低 Python 版本改为 3.10，不再维持 3.8 声明。

> “你这个修复的问题很大，你多次检查一下和验证一下”

修复要求包含复现、对应回归和必要的全量验证；过程发现的问题修正后再测，不通过放宽数值断言交差。

> “你先按顺序全部完成看看，有问题再保留到最后我来解决”

已连续完成剩余 N 项与精简项，将默认优化器和预算政策留待最终讨论。

> “不发布正式v版本，先发到github，因为我要换电脑来完成”

本次只同步 GitHub 开发分支并保存交接材料，不触发正式发版。

## 8. 相关记录与入口

- [当前 TODO 与详细验收记录](TODO.md)
- [第二轮整体包审查](package-review-2026-09-08.md)
- [前一轮问题清单与处理过程](code-review-issues-2026-09-06.md)
- [核函数专项](kernel-review-2026-09-06.md)
- [多目标专项](multiobjective-review-2026-09-07.md)
- [61 项联合回归](../tests/test_remaining_review.py)
- [跨平台 CI](../.github/workflows/ci.yml)
- [干净 wheel 测试脚本](../.github/scripts/test_wheel.py)
- [开发约定](../AGENTS.md)

旧报告中的 `/tmp/...` 是旧电脑临时证据路径，不保证换电脑后存在。本次保留的日志以仓库内 verification 目录为准，其余历史结论可通过已提交回归测试重现。

## 9. 本次交接验证

- 2026-09-18 再次运行本地完整测试：**1345 passed，4 warnings，19.24 秒**，退出码 0。日志：[本次全量验证](verification/0918-py312-pytest.txt)。
- 精简测量的原始汇总：[自核数组与 artifact 大小](verification/0918-simplification-audit.json)。
- 本次交接没有额外修改算法行为；整理了忽略规则、提交范围和可跨电脑阅读的文档链接。
- 虚拟环境、编译产物、覆盖率数据库、系统杂项和本机会话数据不属于开发源码；新电脑重建环境即可。仓库已有的此类跟踪项按本次整理从 Git 索引移除，本地仍存在的文件保留。
- 网络：按用户要求直连 GitHub，本次 Git HTTPS 命令临时禁用旧本地代理，不修改全局网络配置。
- GitHub 是否推送成功以实际远程分支核对为准，不将本地提交或测试通过等同于上传成功。远程 CI 的最终状态查看对应提交的 Actions。
