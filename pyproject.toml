[build-system]
requires = [
    "setuptools",
    "scipy",
    "wheel",
    "Cython",
    "pybind11",
    "numpy",
    
]
build-backend = "setuptools.build_meta"

[project]
name="UQPyL"
version="2.0.1"
dependencies = [
  "numpy",
  "scipy",
]